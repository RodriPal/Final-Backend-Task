const userController = require('./userController');
const animeChapterController = require('./animeChapterController');
const animeSeriesController = require('./animeSeriesController');

module.exports = {
    userController,
    animeChapterController,
    animeSeriesController
}